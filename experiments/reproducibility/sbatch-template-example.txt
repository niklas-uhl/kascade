#!/bin/bash
#SBATCH -o ${output_log}
#SBATCH -e ${error_output_log}
#SBATCH -J ${job_name}
#SBATCH --time=${time_string}

# add your module setup here

# this will expand to the actual job commands,
# using command-template-example.txt as a template
${commands}
